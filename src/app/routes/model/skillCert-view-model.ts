import { ViewModel } from '@utils';

/**
 * 技能证书情况
 */
export class SkillCertViewModel extends ViewModel {
    /**
     * 时间
     */
    obtainedTime;
    /**
     * 名称
     */
    jobName;
    /**
     * 等级
     */
    level;
}
