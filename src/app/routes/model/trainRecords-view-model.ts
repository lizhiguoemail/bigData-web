import { ViewModel } from '@utils';

/**
 * 培训记录
 */
export class TrainRecordsViewModel extends ViewModel {
    /**
     * 时间
     */
    obtainedTime;
    /**
     * 机构
     */
    trainOrg;
    /**
     * 工种
     */
    level;
}
